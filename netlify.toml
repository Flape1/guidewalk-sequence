[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"
  NODE_VERSION = "20.11.1"
  NODE_ENV = "production"
  NETLIFY_BUILD_DEBUG = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 